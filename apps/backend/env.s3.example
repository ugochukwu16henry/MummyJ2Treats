# S3-compatible storage (e.g. Railway / t3.storageapi.dev)
# When set, all uploads (pictures, videos, receipts) go to the bucket instead of local disk.
#
# Railway: Add these as service Variables. If the build fails with
# "failed to stat .../secrets/S3_ENDPOINT", remove S3_* from Variables or add placeholder values.

# Endpoint URL
S3_ENDPOINT=https://t3.storageapi.dev

# Region
S3_REGION=auto

# Bucket Name
S3_BUCKET=versatile-organizer-qge5o1

# Access Key ID
S3_ACCESS_KEY_ID=your_access_key_id

# Secret Access Key
S3_SECRET_ACCESS_KEY=your_secret_access_key

# Optional: public URL for reading files (if different from endpoint/bucket).
# S3_PUBLIC_URL=https://t3.storageapi.dev/versatile-organizer-qge5o1
