// Next.js config for Vercel deployment
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add more config as needed for Vercel
};

module.exports = nextConfig;
